apiVersion: v1
kind: ServiceAccount
metadata:
  name: hello-world-frontend
  namespace: hello-world
  labels:
    app.kubernetes.io/name: hello-world-frontend
# The app talks to no Kubernetes APIs, so don't mount a token into the pod.
automountServiceAccountToken: false
